const lowerCaseNames = ["charlie", "rob", "tea", "bert"];

const toCapitalLetter = (arr) => {
  const capitalArray = arr.map.charAt(0).toUpperCase;
  return capitalArray;
};

console.log(lowerCaseNames);

const arrayWithVowels = ["a", "b", "c", "d"];

const removeVowels = (arr) => {
  const withoutVowels = withoutVowels.map((withoutVowels) =>
    withoutVowels.replace(/[aeiou]/g, "")
  );
  return withoutVowels;
};
removeVowels();

console.log(removeVowels(arrayWithVowels));
